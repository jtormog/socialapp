/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:['zfk2jgnesvfdleea.public.blob.vercel-storage.com', 'lh3.googleusercontent.com']
    }
};

export default nextConfig;
